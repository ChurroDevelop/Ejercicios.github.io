/**
 * Se requiere calcular la distancia entre dos puntos, realizar el código que permita hallar la
solución, por favor utilizar funciones matemáticas para ello.:
a. rc((x2-x1)^2 + (y2-y1)^2).
 */

let r = parseInt(prompt("Ingrese el valor de r"));
let c = parseInt(prompt("Ingrese el valor de c"));
let x = parseInt(prompt("Ingrese el valor de x"));
let y = parseInt(prompt("Ingrese el valor de y"));

let solucion = (r * c ((Math.pow(x,2) - x) ** 2 + (Math.pow(y,2) - y) ** 2) );
alert(`La solucion de la expresion es: ${solucion}`);